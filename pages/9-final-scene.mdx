# A Scene Testing All New Features

The final scene exercises every feature introduced in this book. It's a large scene — thousands of objects — so the BVH is essential for reasonable performance.

The scene contains:
- Thousands of small spheres on the ground, arranged in a flat grid
- A single motion-blurred sphere
- A sphere with an earth image texture
- A sphere with the Perlin noise texture (marble-like)
- Two quads forming a light source on the ceiling
- A glass sphere
- A box of spheres rendered through a BVH (a "cloud" of spheres)

<Tabs>
  <Tab label="Rust">
    ```rust filename="main.rs | Final scene"
    pub fn final_scene(image_width: u32, samples_per_pixel: u32, max_depth: i32) {
        let mut world = HittableList::new();

        // Ground: many small random-colored boxes
        let mut boxes1 = HittableList::new();
        let ground = Arc::new(Lambertian::from_color(Color::new(0.48, 0.83, 0.53)));
        let boxes_per_side = 20;
        for i in 0..boxes_per_side {
            for j in 0..boxes_per_side {
                let w = 100.0;
                let x0 = -1000.0 + i as f64 * w;
                let z0 = -1000.0 + j as f64 * w;
                let y0 = 0.0;
                let x1 = x0 + w;
                let y1 = random_double_range(1.0, 101.0);
                let z1 = z0 + w;
                boxes1.add(Arc::new(make_box(
                    Point3::new(x0, y0, z0),
                    Point3::new(x1, y1, z1),
                    ground.clone(),
                )));
            }
        }
        world.add(Arc::new(BvhNode::from_list(boxes1)));

        // Light
        let light = Arc::new(DiffuseLight::from_color(Color::new(7.0, 7.0, 7.0)));
        world.add(Arc::new(Quad::new(
            Point3::new(123.0, 554.0, 147.0),
            Vec3::new(300.0, 0.0, 0.0),
            Vec3::new(0.0, 0.0, 265.0),
            light,
        )));

        // Motion blur sphere
        let center1 = Point3::new(400.0, 400.0, 200.0);
        let center2 = center1 + Vec3::new(30.0, 0.0, 0.0);
        let sphere_mat = Arc::new(Lambertian::from_color(Color::new(0.7, 0.3, 0.1)));
        world.add(Arc::new(MovingSphere::new(center1, center2, 0.0, 1.0, 50.0, sphere_mat)));

        // Glass sphere
        world.add(Arc::new(Sphere::new(
            Point3::new(260.0, 150.0, 45.0), 50.0,
            Arc::new(Dielectric::new(1.5)),
        )));

        // Metal sphere
        world.add(Arc::new(Sphere::new(
            Point3::new(0.0, 150.0, 145.0), 50.0,
            Arc::new(Metal::new(Color::new(0.8, 0.8, 0.9), 1.0)),
        )));

        // Subsurface sphere (glass + constant medium)
        let boundary = Arc::new(Sphere::new(
            Point3::new(360.0, 150.0, 145.0), 70.0,
            Arc::new(Dielectric::new(1.5)),
        ));
        world.add(boundary.clone());
        world.add(Arc::new(ConstantMedium::new(boundary, 0.2, Color::new(0.2, 0.4, 0.9))));

        // Fog sphere around the whole scene
        let boundary2 = Arc::new(Sphere::new(
            Point3::new(0.0, 0.0, 0.0), 5000.0,
            Arc::new(Dielectric::new(1.5)),
        ));
        world.add(Arc::new(ConstantMedium::new(boundary2, 0.0001, Color::new(1.0, 1.0, 1.0))));

        // Earth sphere
        let earth_texture = Arc::new(ImageTexture::new("earthmap.jpg"));
        world.add(Arc::new(Sphere::new(
            Point3::new(400.0, 200.0, 400.0), 100.0,
            Arc::new(Lambertian::new(earth_texture)),
        )));

        // Perlin noise sphere
        let perlin_texture = Arc::new(NoiseTexture::new(0.2));
        world.add(Arc::new(Sphere::new(
            Point3::new(220.0, 280.0, 300.0), 80.0,
            Arc::new(Lambertian::new(perlin_texture)),
        )));

        // Box of spheres (cloud)
        let mut boxes2 = HittableList::new();
        let white = Arc::new(Lambertian::from_color(Color::new(0.73, 0.73, 0.73)));
        for _ in 0..1000 {
            boxes2.add(Arc::new(Sphere::new(
                Point3::random_range(0.0, 165.0), 10.0, white.clone(),
            )));
        }
        world.add(Arc::new(Translate::new(
            Arc::new(RotateY::new(Arc::new(BvhNode::from_list(boxes2)), 15.0)),
            Vec3::new(-100.0, 270.0, 395.0),
        )));

        // Camera
        let mut cam = Camera::default();
        cam.aspect_ratio = 1.0;
        cam.image_width = image_width;
        cam.samples_per_pixel = samples_per_pixel;
        cam.max_depth = max_depth;
        cam.background = Color::new(0.0, 0.0, 0.0);
        cam.vfov = 40.0;
        cam.lookfrom = Point3::new(478.0, 278.0, -600.0);
        cam.lookat = Point3::new(278.0, 278.0, 0.0);
        cam.vup = Vec3::new(0.0, 1.0, 0.0);
        cam.defocus_angle = 0.0;
        cam.render(&world);
    }
    ```
  </Tab>

  <Tab label="C++">
    ```cpp filename="main.cc | Final scene"
    void final_scene(int image_width, int samples_per_pixel, int max_depth) {
        hittable_list boxes1;
        auto ground = make_shared<lambertian>(color(0.48, 0.83, 0.53));
        int boxes_per_side = 20;
        for (int i = 0; i < boxes_per_side; i++) {
            for (int j = 0; j < boxes_per_side; j++) {
                auto w = 100.0;
                auto x0 = -1000.0 + i*w;
                auto z0 = -1000.0 + j*w;
                auto y0 = 0.0;
                auto x1 = x0 + w;
                auto y1 = random_double(1,101);
                auto z1 = z0 + w;
                boxes1.add(box(point3(x0,y0,z0), point3(x1,y1,z1), ground));
            }
        }
        hittable_list world;
        world.add(make_shared<bvh_node>(boxes1));

        auto lights = make_shared<diffuse_light>(color(7,7,7));
        world.add(make_shared<quad>(point3(123,554,147), vec3(300,0,0), vec3(0,0,265), lights));

        // ... remaining objects ...

        camera cam;
        cam.aspect_ratio = 1.0;
        cam.image_width = image_width;
        cam.samples_per_pixel = samples_per_pixel;
        cam.max_depth = max_depth;
        cam.background = color(0,0,0);
        cam.vfov = 40;
        cam.lookfrom = point3(478, 278, -600);
        cam.lookat = point3(278, 278, 0);
        cam.vup = vec3(0,1,0);
        cam.defocus_angle = 0;
        cam.render(world);
    }
    ```
  </Tab>
</Tabs>

The final render showcases the complete feature set: motion blur, BVH acceleration, checker and image and Perlin textures, quads, area lights, instances, and participating media all working together.

Congratulations — you now have a "real" ray tracer! The features introduced in this book are the foundation of most production path tracers. From here, further reading might cover importance sampling, spectral rendering, bidirectional path tracing, and many other advanced topics.
