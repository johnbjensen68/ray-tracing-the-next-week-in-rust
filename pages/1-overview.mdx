# Overview

Welcome! This is a tutorial to learn Rust by implementing an advanced ray tracer, based on the book [Ray Tracing: The Next Week](https://raytracing.github.io/books/RayTracingTheNextWeek.html) by Peter Shirley, Trevor David Black, and Steve Hollasch. It is the sequel to [Ray Tracing in One Weekend](https://raytracing.github.io/books/RayTracingInOneWeekend.html), and this tutorial assumes you have already completed [The Ray Tracing Road to Rust](https://the-ray-tracing-road-to-rust.vercel.app/) by [Sheng Chou](https://github.com/chousheng) (or have equivalent knowledge).

This site is a direct continuation of Sheng's project. The platform, the side-by-side Rust/C++ tab format, the chapter scaffolding, and the convention of treating the book's narrative as the source of truth all come from *The Ray Tracing Road to Rust*. If you found that tutorial useful, this one is meant to look and read the same way — that's by design.

In this installment we add textures, volumes (like fog), lights, quadrilateral primitives, instances, and a bounding volume hierarchy (BVH) to make the renderer fast enough for complex scenes.

We adapt the original C++ book into Rust, provide a reference Rust implementation, and add notes on Rust-specific features and idioms. You can switch between Rust and C++ code with the interactive tabs to compare the two languages side by side.

Along the way, you'll encounter:

*   Rust traits and generics applied to an extensible material/texture system

*   Smart pointers (`Arc<T>`, `Box<T>`) for sharing scene objects

*   Recursive data structures for the BVH tree

*   Procedural texture generation with Perlin noise

*   Coordinate transformations with instances

*   Participating media (fog/smoke) via probabilistic ray scattering

Have fun!
